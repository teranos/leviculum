# Baseline: rncp (Python tools) through all-Python topology
# Measures Python rncp transfer performance for comparison with lrncp.

[test]
name = "lrncp_baseline"
description = "Baseline: Python rncp through all-Python topology"
timeout_secs = 600

[nodes.alpha]
type = "python"
respond_to_probes = true

[nodes.relay]
type = "python"
respond_to_probes = false

[nodes.charlie]
type = "python"
respond_to_probes = true

[links]
alpha-relay = "tcp"
charlie-relay = "tcp"

[[steps]]
action = "sleep"
duration_secs = 20

[[steps]]
action = "wait_for_path"
on = "alpha"
destination = "charlie.probe"
timeout_secs = 60

[[steps]]
action = "file_transfer"
sender = "alpha"
receiver = "charlie"
sender_tool = "rncp"
receiver_tool = "rncp"
file_sizes = [102400, 1048576]
direction = "a_to_b"
repeats = 3
timeout_secs = 300
